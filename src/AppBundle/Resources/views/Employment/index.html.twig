{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Employment list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Company</th>
                <th>Position</th>
                <th>Responsibilities</th>
                <th>Startdate</th>
                <th>Enddate</th>
                <th>Creationtimestamp</th>
                <th>Active</th>
                <th>Deleted</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('employment_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.company }}</td>
                <td>{{ entity.position }}</td>
                <td>{{ entity.responsibilities }}</td>
                <td>{% if entity.startDate %}{{ entity.startDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.endDate %}{{ entity.endDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.creationTimestamp %}{{ entity.creationTimestamp|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.active }}</td>
                <td>{{ entity.deleted }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('employment_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('employment_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('employment_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
